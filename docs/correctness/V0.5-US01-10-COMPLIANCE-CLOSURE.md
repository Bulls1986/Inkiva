# v0.5 US01-US10 Compliance Closure

## Purpose

Re-audit US01 through US10 from the current `develop` state and close the remaining process, architecture, combined-state, and acceptance gaps without weakening tests or repository gates.

## Task identity

- Branch: `audit/v0.5-us01-10-governance`
- Base: `origin/develop@f9bf06f48c7b76469090a0d4a0a8c868f85db430`
- Source of truth: repository state and executable validation, not prior chat summaries.
- Governing contracts read before any production-code mutation:
  - `AGENTS.md`
  - `docs/agent/WORKFLOW.md`
  - `docs/agent/ENVIRONMENT.md`
  - `docs/agent/TESTING.md`
  - `docs/agent/ARCHITECTURE_RELEASE.md`
  - `docs/architecture/README.md`
  - `docs/architecture/ARCH-07-BACKGROUND-SCHEDULER.md`

## Stage 0 — latest-develop reality check

Status: **complete**

Observed:

- Current `develop` is clean at `f9bf06f` (`docs: close US08 merge record (#202)`).
- The previously discussed unified US01-US10 closure record was not present on current `develop`; it therefore cannot be treated as completed repository evidence.
- `ARCH-TODO-0.5-01` is still open. `RecoveryCenterSession` directly constructs `DocumentIntelligenceService` in `replaceFile()`.
- US06 branch head `5dbe851` is not an ancestor of current `develop`; US06 must not be counted as integrated merely because its worktree is clean.
- No active WebCodex Jobs existed before this audit started.

Conclusion:

The prior conversational closure state was stale relative to the repository. This audit restarts from current `develop` and records only evidence that is present or newly executed.

## Stage 1 — ARCH-TODO-0.5-01 recovery/history boundary

Status: **implementation complete; clean-CI validation pending**

Affected boundary:

- Recovery Center must depend on a narrow history-persistence operation rather than the concrete `DocumentIntelligenceService`.
- Local History remains the owner of snapshot semantics/storage.
- The replacement sequence must remain: disk reread → external-change guard → pre-replace history snapshot → atomic write.
- Autosave/background scheduling ownership is unchanged.

Environment/readiness evidence:

- Node `24.21.0` and pnpm `10.33.4` verified.
- The fresh worktree has no local dependency graph; a matching root dependency graph exists in the main checkout.
- Managed worktree bootstrap returned the documented `managed_worktree_root_unavailable`; the canonical Git-native `.worktrees` path was used instead.
- Both the package-manager Vitest launch and the direct Vitest CLI launch timed out before test discovery. They are environment/bootstrap evidence, not product failures and not Red evidence.
- Production source remained unchanged until the independent native Node architecture contract produced valid Red evidence.

Strict test-first evidence:

- Added `scripts/recovery-history-boundary.test.mjs` before production mutation.
- Red command: `node --test scripts/recovery-history-boundary.test.mjs`.
- The runner discovered and executed exactly one intended test and failed on the product/architecture assertion that the external Recovery History persistence boundary did not exist. This is valid Red evidence.
- Only after that Red result, added the narrow boundary and changed `RecoveryCenterSession`.
- Green command: the identical `node --test scripts/recovery-history-boundary.test.mjs` passed `1/1`.

Implementation:

- Added `RecoveryHistoryPersistence` and `RecoveryHistoryPersistenceFactory` under `main/documentIntelligence`.
- The default factory owns construction of `DocumentIntelligenceService` and the `local-history` root.
- Recovery Center now depends only on `createSnapshot` and receives the factory through constructor injection.
- Added a Vitest behavior test requiring the injected boundary to receive disk V1 before V2 replacement.
- Wired the architecture contract into root `typecheck`, making the boundary part of the standard lint/typecheck PR gate.

Validation limitation:

- Two focused Vitest launches timed out before test discovery; neither is Red/product evidence.
- A direct `vue-tsc` attempt failed during dependency loading on the reused donor graph (`estree-walker` package export), before project type checking.
- Per the environment contract, these launch/bootstrap failures are not being retried with random dependency changes. Clean CI must supply the behavior-unit and full typecheck evidence before final closure.
- Post-implementation local gates are green for the architecture contract (`1/1`), `package.json` parse, and `git -c core.whitespace=cr-at-eol diff --check`.

## Stage 2 — US04 data-safety closure

Status: **implementation complete; behavior-suite/CI validation pending**

Strict test-first evidence:

- Added three focused behavior cases before production mutation: rollback pruned by byte quota, rollback pruned by age retention, and UTF-8 BOM + CRLF byte-faithful restore.
- Added `scripts/us04-history-data-safety.test.mjs` as the executable architecture/data-safety contract.
- Red: the native Node test discovered and executed exactly one intended test and failed because `LocalHistoryRollbackUnavailableError` / retained-rollback protection did not exist.
- Green: the same contract passed `1/1`; `git -c core.whitespace=cr-at-eol diff --check` also passed.

Implementation:

- A restore-created `before-restore` snapshot is now read back after store pruning; if quota/retention removed it, restore throws and does not write the Markdown file.
- Local History snapshots now carry `isBom` end-to-end through renderer state, IPC validation, shared types and storage.
- Restore materializes target bytes from snapshot metadata, preserving LF/CRLF and UTF-8 BOM semantics rather than blindly writing normalized UTF-8 text.
- Existing stale-preview protection remains ahead of rollback creation and disk mutation.

Local Vitest remains unavailable in this worktree because the already-diagnosed dependency bootstrap stalls before discovery. The new behavior cases are therefore mandatory evidence in the clean final matrix/CI and are not being replaced by a weaker local claim.

## Stage 3 — US01 AC-73 close transaction

Status: **implementation complete; behavior-suite/CI validation pending**

Strict test-first evidence:

- Added a two-dirty-document close test for `startUpAction=restoreAll` before production mutation.
- Added `scripts/us01-ac73-close-transaction.test.mjs`.
- Red: the contract executed and failed on the explicit `startUpAction !== 'restoreAll'` bypass.
- Green: after the fix, the same contract passed `1/1`; whitespace gate passed.

Implementation:

- Whole-window close now routes every non-empty dirty-document set through `mt::close-window-confirm`, regardless of startup preference.
- `restoreAll` is no longer treated as implicit permission to close dirty documents.
- This preserves the existing Save / Keep for Recovery / Discard transaction and its per-document revision checks while removing the startup-preference shortcut.


## Stage 4 — US05 AC-76 startup target × layout policy

Status: **implementation complete; behavior-suite/clean-CI validation pending**

Strict test-first evidence:

- Added focused startup/layout behavior cases for all four startup targets and both layout-policy branches.
- Added `scripts/us05-ac76-startup-layout.test.mjs`.
- Red: the contract first failed on the stale renderer `StartUpAction` union (`lastSession` instead of `openLastFolder`/`folder`).
- Green: the final contract passes `1/1`; whitespace gate passes.
- Focused Vitest did not reach discovery because this reused worktree has no runnable `vitest` binary (`ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL Command "vitest" not found`). This is environment evidence, not a product failure; the behavior suite remains mandatory in clean CI.

Implementation:

- Renderer startup-action typing now matches the main-process schema: `restoreAll | openLastFolder | folder | blank`.
- `restoreLayoutState=false` no longer reuses the previous native window bounds; startup uses the default 1200×800 window geometry.
- Blank-layout startup uses default sidebar/tab-bar visibility, default sidebar width, default right-column/split state, and skips buffered layout restoration.
- Per-tab source/WYSIWYG mode and semantic viewport remain document-owned and are preserved; outline collapse is treated as layout state and reset under blank-layout startup.
- `sourceCodeModeEnabled` remains a user/new-document preference and is no longer incorrectly coupled to blank-layout policy.
- Final diff review found the same coupling still present in the `reload()` bootstrap path. The AC-76 contract was tightened first, produced a valid Red, then `reload()` was corrected; the focused native bundle returned to `4/4` Green.

## Stage 5 — US09 real editor-feel E2E

Status: **real interaction E2E authored; clean-CI execution pending**

Coverage added:

- Added `packages/desktop/test/e2e/us09-editor-feel.spec.ts` using real Playwright mouse/keyboard events rather than programmatic selection injection.
- Covers hover I-beam without focus theft, click-to-caret followed by real typing, double-click word selection, drag selection, and Shift-click selection extension while keeping the operation scoped to the active document.
- Existing `anchor-link-scroll.spec.ts` remains the paired real-window evidence for plain-link caret behavior versus Cmd/Ctrl-click navigation.
- Pre-commit review fixed a strict-locator defect in the new E2E helper (`PARAGRAPH` can match multiple elements); coordinate lookup is now explicitly scoped to the first paragraph.

Validation-path evidence:

- Local Playwright attempt started but never reached test discovery: `0 tests detected`, no stdout/stderr, then bounded timeout.
- Readiness diagnosis proved the current worktree has no Playwright, Vitest, or Electron dependency graph/launcher.
- No registered warm worktree provides a complete matching Playwright/Vitest graph. The main checkout is also not a valid donor for a complete root graph.
- The single canonical recovery attempt, `corepack pnpm install --offline --frozen-lockfile --ignore-scripts`, likewise remained at bootstrap with no output until bounded timeout.
- Per `AGENTS.md` / environment contract, these are environment/bootstrap findings, not US09 product failures. No further local install variants will be tried; real Electron execution is mandatory in clean CI.
## Stage 6 — pre-matrix discovered-issue closure

Status: **local architecture/diff/hygiene review complete; no additional product gap remains open locally**

Latest-develop / US06 truth check:

- The local tracking ref was stale: local `origin/develop` was `f9bf06f`, while GitHub `develop` was already `dcdfda9`.
- GitHub compare proved the missing remote commit was `dcdfda9 feat: close v0.5 US06 settings behavior (#200)`, including the US06 validation/closure history.
- After an explicit fetch, the governance branch was rebased onto `dcdfda9`. The only rebase conflict was the correctness-document index; both the US06 entry and this compliance-closure entry were retained. No product-code conflict occurred.
- Post-rebase focused native contracts remained `4/4` Green.

Recovery replacement rollback retention:

- Reverse data-safety review found that Recovery Center replacement created a `before-external-change` Local History snapshot but did not prove that retention/quota pruning kept it before overwriting the disk file.
- Red: the architecture contract failed because `RecoveryHistoryPersistence` did not read back the snapshot it had just created.
- Fix: the narrow persistence boundary now creates the snapshot, reads it back by id, and throws `RecoveryHistorySnapshotUnavailableError` if it was pruned. Recovery Center already awaits this boundary, so failure aborts before the atomic disk replacement and leaves the recovery item pending.
- Added behavior coverage that a persistence-boundary failure preserves disk V1 and leaves the recovery revision available.
- Green: the boundary contract returned to `1/1`, and the focused native bundle remained `4/4` Green.

Architecture / hygiene closure:

- Final committed-range review from current `develop` (`dcdfda9`) through local governance HEAD found 27 changed files: 14 production, 9 test, 4 docs/config-related classifications, with no binary files and no architecture warning signals from the deterministic review map.
- Targeted anti-regression search found no stale `lastSession` startup value under desktop source and no direct `new DocumentIntelligenceService(...)` dependency under the Recovery Center session boundary.
- `restoreLayoutState` ownership remains explicit: native window geometry/sidebar/tab-bar are layout policy, per-tab mode/viewport remain document state, and source-mode new-document preference remains independent.
- Workspace hygiene passed clean after Stage 6 commit: no untracked temp/smoke/cache files, no suspicious tracked paths, no staged/conflicted state.

## Stage 7 — final US01-US10 combined-state matrix

Status: **local non-dependency gates complete; canonical dependency-backed gates require clean CI**

Authoritative local evidence on the final combined state:

- Native architecture/data-safety matrix: `16/16` Green across ARCH-08 closure, CORRECTNESS-01 readiness, Muya public type boundary, Recovery/History boundary, US01 AC-73, US04 data safety, and US05 AC-76.
- Committed-range whitespace gate: `git -c core.whitespace=cr-at-eol diff origin/develop...HEAD --check` Green.
- `corepack pnpm run typecheck` partially executed successfully: Muya public type boundary Green, Recovery/History boundary Green, and `@muyajs/core build:types` completed. It then stopped before desktop type analysis because `vue-tsc` is absent from this worktree dependency graph.
- `corepack pnpm run lint` reached the repository lint script but stopped before linting because `eslint` is absent.
- `corepack pnpm run test:unit` reached the desktop unit script but executed zero test bodies because `vitest` is absent.
- `corepack pnpm run test:e2e` reached the desktop E2E script but executed zero Playwright tests because `playwright` is absent.
- These three launcher failures match the already diagnosed missing-`node_modules` topology. They are validation-infrastructure blockers, not product Red evidence, and the repository contract forbids weakening or substituting the missing canonical gates.

## Remaining closure scope

- Run lint, complete desktop `vue-tsc`, full unit suite, and full Electron E2E—including `us09-editor-feel.spec.ts`—in canonical clean CI.
- Mark `ARCH-TODO-0.5-01` fully closed only after that clean-CI behavior/type evidence is Green.
- Push the rebased governance branch, create/update the PR, follow all CI gates, then squash-merge only after every required gate is Green.
- After merge, fetch authoritative remote `develop`, verify the squash commit is present, rerun the final post-merge smoke/contract evidence, and update this record to final closed status.

The full local matrix has no known product failure. Final release acceptance remains intentionally blocked on canonical clean-CI evidence rather than being inferred from local static/native coverage.
